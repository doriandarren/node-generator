


const generateModuleStandard = async(selectedComponents, singularName, pluralName, columns, projectPath) => {
    
    console.log(
        selectedComponents,
        nameProject,
        projectPath,
        singularName,
        pluralName,
        columns
    );



}