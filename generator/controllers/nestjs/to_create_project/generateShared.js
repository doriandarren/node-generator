import fs from "fs";
import path from "path";
import { createFolder } from "../../../helpers/helperFile.js";

export const generateShared = async (fullPath) => {
  
  //TODO agregar llamadas hascia generateModuleStandardNestJS

};
